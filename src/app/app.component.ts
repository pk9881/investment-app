/**
 * The main application component for the investment app.
 *
 * This component serves as the root component for the Angular application.
 * It is defined with the selector 'app-root' and uses the template specified
 * in the 'app.component.html' file.
 *
 * @component
 * @selector app-root
 * @templateUrl ./app.component.html
 */
import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {}
